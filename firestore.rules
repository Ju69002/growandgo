
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    function isSignedIn() {
      return request.auth != null;
    }

    function isOwner(userId) {
      return isSignedIn() && request.auth.uid == userId;
    }

    // Gestion des profils utilisateurs
    match /users/{userId} {
      // Lecture autorisée pour l'auth, mais écriture restreinte au propriétaire exact
      allow read: if isSignedIn();
      allow write: if isOwner(userId); 
      
      match /received_invoices/{invoiceId} {
        allow read: if isOwner(userId);
        allow write: if isSignedIn();
      }
    }

    // Gestion des entreprises et de leurs sous-collections
    match /companies/{companyId} {
      allow read: if isSignedIn();
      // On autorise l'écriture si l'utilisateur est authentifié. 
      // Idéalement on vérifierait l'appartenance à la compagnie ici.
      allow write: if isSignedIn();

      // Règle de stabilisation : vérification du userId sur les ressources sensibles (événements, docs)
      match /events/{eventId} {
        allow read: if isSignedIn();
        allow write: if isSignedIn() && (
          // Création ou mise à jour autorisée si le userId correspond à l'auth
          request.resource.data.userId == request.auth.uid ||
          // Ou si le document existant appartient déjà à l'utilisateur
          resource.data.userId == request.auth.uid
        );
      }

      match /documents/{docId} {
        allow read: if isSignedIn();
        allow write: if isSignedIn();
      }

      match /categories/{catId} {
        allow read: if isSignedIn();
        allow write: if isSignedIn();
      }

      match /{allSubcollections=**} {
        allow read, write: if isSignedIn();
      }
    }

    // Autoriser la lecture des modules (statique)
    match /modules/{moduleId} {
      allow read: if true;
    }
  }
}

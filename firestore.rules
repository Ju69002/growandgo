
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    function isSignedIn() {
      return request.auth != null;
    }

    // Gestion des profils utilisateurs
    match /users/{userId} {
      // Autoriser la lecture publique des IDs pour le répertoire (facilite les tests du prototype)
      allow list: if true;
      // Un utilisateur peut lire et modifier son propre profil
      allow get, update: if isSignedIn() && request.auth.uid == userId;
      // Autoriser la création de son propre profil
      allow create: if isSignedIn() && request.auth.uid == userId;
      // Les super admins peuvent tout faire
      allow read, write: if isSignedIn() && exists(/databases/$(database)/documents/users/$(request.auth.uid)) && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'super_admin';
    }

    match /companies/{companyId} {
      // Tout le monde peut lire le nom d'une entreprise pour l'affichage
      allow read: if true;
      // Autoriser la création d'entreprise pour les nouveaux inscrits (Patrons)
      allow create: if isSignedIn();
      // Seul le super admin ou les membres peuvent modifier
      allow update: if isSignedIn();
    }

    function belongsToCompany(companyId) {
      return isSignedIn() && (
        (exists(/databases/$(database)/documents/users/$(request.auth.uid)) && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.companyId == companyId) || 
        (exists(/databases/$(database)/documents/users/$(request.auth.uid)) && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'super_admin')
      );
    }

    match /companies/{companyId}/categories/{categoryId} {
      allow read: if belongsToCompany(companyId);
      allow write: if belongsToCompany(companyId);
    }

    match /companies/{companyId}/documents/{documentId} {
      allow read: if belongsToCompany(companyId);
      allow write: if belongsToCompany(companyId);
    }

    match /companies/{companyId}/events/{eventId} {
      allow read: if belongsToCompany(companyId);
      allow write: if belongsToCompany(companyId);
    }
  }
}
